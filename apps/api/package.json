{
  "name": "@picsearch/api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: API gateway, ingestion pipeline, orchestrator agent, retrieval + reranking.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@picsearch/shared": "workspace:*",
    "hono": "^4.8.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.22.0"
  }
}
