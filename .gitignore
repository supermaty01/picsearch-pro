# dependencies
node_modules/

# build output
dist/
.wrangler/

# environment & secrets — never commit
.env
.env.*
!.env.example
# exception: the web prod config holds only the public API origin (docs/10)
!apps/web/.env.production
.dev.vars
!.dev.vars.example

# supabase CLI local state — never commit (contains pooler URL, project ref)
# NOTE: supabase/config.toml and supabase/migrations/ SHOULD be committed.
supabase/.temp/
supabase/.branches/

# test
coverage/

# misc
.DS_Store
*.log
*.local
